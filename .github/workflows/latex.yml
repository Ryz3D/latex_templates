name: PDF

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Install TeX Live
        uses: ryz3d/install-texlive@v1
        with:
          package_file: packages_list.txt
      
      - name: Compile examples
        run: cd examples; for i in *.tex; do pdflatex $i; do pdflatex $i;done
      
      - name: Biber
        run: cd examples; for i in *.tex; do biber $i; do pdflatex $i;done
      
      - name: LaTeX compilation Part two
        run: cd examples; for i in *.tex; do pdflatex $i; do pdflatex $i;done
      
      - name: Archive .pdf
        uses: actions/upload-artifact@v3
        with:
          name: CD_${{github.run_number}}
          path: examples/*.pdf
